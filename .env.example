# PostgreSQL connection string
# Example: postgresql://user:password@localhost:5432/silverforge?schema=public
DATABASE_URL=postgresql://postgres:Sulptanco.1@db.byracxeealbrfaaxhujw.supabase.co:5432/postgres

# Secret used to sign auth session JWTs. Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-string"

# Base URL of the app (used for building absolute links, e.g. store URLs)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
